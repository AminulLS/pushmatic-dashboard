export declare type Host = {
    domain: string
    cdn: string
    api: string
    name: string
    title: string
    desc: string
    favicon: string
    logo: string
    icon: string
}

export declare type HostCollection = {
    [name: string]: Host
}
